# skills/customer-support — Customer Support

Respond to customers and track satisfaction: inbox, tickets, reviews.

Placeholder domain — created ahead of need, no skill lives here yet. Accepted
prefix: `support-*` (e.g. `support-inbox`, `support-review-monitor`). Each
skill lives in its own `support-<name>/` subfolder with at least a
`SKILL.md`, per the convention in [`skills/README.md`](../README.md) and the
registry [`skills/domains.json`](../domains.json).
