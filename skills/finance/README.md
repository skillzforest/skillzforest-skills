# skills/finance — Finance & Billing

Invoice, track payments, and manage subscriptions.

No skill is implemented in this domain yet. Accepted prefix: `billing-*`
(e.g. `billing-invoice`, `billing-subscription`). Each skill lives in its own
`billing-<name>/` subfolder with at least a `SKILL.md`, per the convention in
[`skills/README.md`](../README.md) and the registry
[`skills/domains.json`](../domains.json).
