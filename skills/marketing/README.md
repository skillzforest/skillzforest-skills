# skills/marketing — Marketing & Communication

Get found and generate leads: content, SEO, social media, advertising.

No skill is implemented in this domain yet. Accepted prefix: `growth-*`
(e.g. `growth-content-calendar`, `growth-ad-copy`). Each skill lives in its own
`growth-<name>/` subfolder with at least a `SKILL.md`, per the convention in
[`skills/README.md`](../README.md) and the registry
[`skills/domains.json`](../domains.json).

Note: technical/content SEO lives under `skills/development/seo/`, not here —
see [Development & Engineering](../development/README.md).
