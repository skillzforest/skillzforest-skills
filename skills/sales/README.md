# skills/sales — Sales & Presales

Sell a digital project: quotes, client scoping, presentations, meeting notes.

The only populated domain today — 11 `presales-*` skills (see [`packs/sales/pack.json`](../../packs/sales/pack.json)). Accepted prefix: `presales-`. Each skill lives in its own `presales-<name>/` subfolder with at least a `SKILL.md` and a `skill.json`, per the convention in [`skills/README.md`](../README.md) and the registry [`skills/domains.json`](../domains.json).
