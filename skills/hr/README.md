# skills/hr — HR & Recruiting

Hire and manage people: job postings, candidate screening, onboarding.

Placeholder domain — created ahead of need, no skill lives here yet. Accepted
prefix: `hr-*` (e.g. `hr-job-posting`, `hr-candidate-screening`). Each skill
lives in its own `hr-<name>/` subfolder with at least a `SKILL.md`, per the
convention in [`skills/README.md`](../README.md) and the registry
[`skills/domains.json`](../domains.json).
